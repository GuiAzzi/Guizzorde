import {
	snmConfigCommand,
	snmAdminCommand,
	snmVotesCommand,
	snmTitleCommand,
} from './Sunday Night Movie/index.js';

export const snmCommands = [snmConfigCommand, snmAdminCommand, snmVotesCommand, snmTitleCommand];

export const guildCommands = [];

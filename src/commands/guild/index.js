import {
	snmConfigCommand,
	snmAdminCommand,
	snmVotesCommand,
	snmTitleCommand,
	snmCommand,
	snmRateCommand,
} from './Sunday Night Movie/index.js';

export const snmCommands = [
	snmConfigCommand,
	snmAdminCommand,
	snmVotesCommand,
	snmTitleCommand,
	snmCommand,
	snmRateCommand,
];

export const guildCommands = [];
